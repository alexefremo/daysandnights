<h1>Listing news</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Content</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>
<div id="user_nav">
  <% if user_signed_in? %>
    Signed in as <%= current_user.profile_name %>. Not you?
    <%= link_to "Sign out", destroy_user_session_path , method: :delete%>
  <% else %>
    <%= link_to "Sign up", new_user_registration_path %> or
    <%= link_to "Sign in", new_user_session_path %>
  <% end %>
</div>
  <tbody>
    <% @news.each do |news| %>
      <tr>
        <td><%= news.title %></td>
        <td><%= news.content %></td>
        <td><%= link_to 'Show', news %></td>
        <td><%= link_to 'Edit', edit_news_path(news) %></td>
        <td><% if user_signed_in? %><% if current_user.right == 'company' or current_user.right == 'administrator' %><%= link_to 'Destroy', news, method: :delete, data: { confirm: 'Are you sure?' } %><% end %><% end %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<% if user_signed_in? %>
<%= link_to 'New News', new_news_path %>  
<% end %>

